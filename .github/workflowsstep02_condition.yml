name: step02_condition
on: workflow_dispatch
jobs:
  build:
    if: github.repository == 'nextlife-dev/github-action-test'
    runs-on: ubuntu-latest
    steps:
    - name: Print Result
      run: |
        echo "Run Build on nextlife-dev/github-action-test"
